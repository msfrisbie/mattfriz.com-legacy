'use strict';

angular.module('mattfrizApp.directives.content', [])
.directive('content', function() {
  return {
    restrict: 'E',
    link: function(scope, el, attrs) {
      // scope.state
    }
  };
});
